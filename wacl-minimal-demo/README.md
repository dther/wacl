# wacl-minimal test
> a hack by dther

WE'RE BRINGING TCL TO THE WEB, BABY.

This is a FUNCTIONAL "tclsh in the web browser"!
The event loop works, and `after` even counts milliseconds correctly!

Right now, everything goes straight to the JS console, though,
and there's a lot of "can't find (thing) stderr output.

BUT IT WORKS! It just needs a little plumbing!
